import { expect } from 'chai'
import { FastWriter } from '../src/services/fast-writer/fast-writer.class.js'

// Include basic testing and the check for the methods in the next block
describe('Testing in one two', () => {
  it('should be a class', () => {
    expect(FastWriter).to.be.a('function')
  })
})
