{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Circuit",
            "program": "${workspaceFolder}/circuit/target/debug/circuit",
            "args": [
                "--dev"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}