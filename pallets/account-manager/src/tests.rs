use super::*;
use crate::mock::{AccountManager, ExtBuilder, Test};
use codec::Decode;
use frame_support::{assert_err, assert_noop, assert_ok};
use frame_system::Origin;
use sp_runtime::DispatchError;

#[test]
fn test_hello() {
    assert!(true)
}
